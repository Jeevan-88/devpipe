version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    container_name: devpipe-backend

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    container_name: devpipe-frontend